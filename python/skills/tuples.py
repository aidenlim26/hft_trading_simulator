# Tuples are lists that can't be changed/edited
# You can only find information about tuples, but can't change it

numbers = (1,2,3)       # Tuples uses curved brackets, lists uses square brackets

print(numbers[0])